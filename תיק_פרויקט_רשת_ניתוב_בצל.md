# תיק פרויקט — רשת ניתוב בצל אנונימית

---

**שם הפרויקט:** רשת ניתוב בצל (Onion Routing Network)

**שם התלמיד:** תמיר

**ת.ז.:** s1522711

**שם החלופה:** הנדסת תוכנה 883589

**תאריך ההגשה:** 25/05/2026

**שמות המנחים:** ——

---

## תוכן עניינים

1. [מבוא](#1-מבוא)
2. [הגדרות](#2-הגדרות)
3. [מבנה / ארכיטקטורה של הפרויקט](#3-מבנה--ארכיטקטורה-של-הפרויקט)
   - [3.3 זרימת המידע — בניית מעגל (CIRCUIT_SETUP)](#33-זרימת-המידע--בניית-מעגל-circuit_setup)
   - [3.7 פרוטוקול הניתוב — פירוט מלא של הודעות וחבילות](#37-פרוטוקול-הניתוב--פירוט-מלא-של-הודעות-וחבילות)
4. [מדריך למשתמש](#4-מדריך-למשתמש)
5. [מדריך למפתח](#5-מדריך-למפתח)
6. [בסיס הנתונים](#6-בסיס-הנתונים)
7. [סיכום אישי / רפלקציה](#7-סיכום-אישי--רפלקציה)
8. [ביבליוגרפיה](#8-ביבליוגרפיה)
9. [נספחים](#9-נספחים)

---

## 1. מבוא

### תיאור תכולת הספר

תיק פרויקט זה מתאר מערכת תוכנה שמממשת רשת ניתוב בצל (Onion Routing) אנונימית בדומה לרשת Tor, יחד עם שרת צ'אט רב-חדרי ולקוח צ'אט גרפי מלא. המסמך כולל תיאור ארכיטקטורת המערכת, פרוטוקולי התקשורת, אלגוריתמי ההצפנה, ותיעוד מפורט של כל רכיב בקוד.

### הרקע לפרויקט

בעולם הדיגיטלי של ימינו, פרטיות ואנונימיות ברשת הפכו לנושאים קריטיים. רשת Tor (The Onion Router) היא הפרויקט הידוע ביותר בתחום — מיליוני משתמשים ברחבי העולם משתמשים בה מדי יום כדי לגלוש באינטרנט מבלי שספקי האינטרנט, ממשלות, או גופים עוינים יוכלו לעקוב אחר הפעילות שלהם.

הרעיון המרכזי של ניתוב בצל הוא שכל הודעה מוצפנת במספר שכבות, כמו קליפות של בצל. כל צומת ברשת מקלף שכבה אחת ומעביר את ההודעה הלאה, מבלי לדעת מי השולח המקורי ומה תוכן ההודעה.

### תהליך המחקר

**סקירת המצב הקיים בשוק:**
- **Tor Browser** — המימוש המקורי, מבוסס C++, משתמש בפרוטוקול ntor (Diffie-Hellman אליפטי).
- **I2P** — רשת אנונימיות מבוזרת עם גישה שונה (tunnels דו-כיווניים).
- **Freenet** — רשת P2P אנונימית לשיתוף קבצים.

**חידושים בפרויקט:**
- מימוש מלא בפייתון בלבד — ללא כלי צד שלישי לניתוב הבצל עצמו.
- שילוב שרת צ'אט רב-חדרי מעל הרשת האנונימית.
- לקוח גרפי (GUI) שמסתיר מהמשתמש את כל מורכבות הניתוב.
- מנגנון **poll** ייחודי המאפשר קבלת הודעות דחויות (push) דרך מעגל שהוא מטבעו request/response.

### אתגרים מרכזיים

**הבעיה:** רשת Tor היא מטבעה request/response — הלקוח שולח הודעה, וצומת היציאה מחכה לתשובת השרת. אבל בצ'אט, השרת צריך לדחוף הודעות אקטיבית ללקוח (כאשר משתמש אחר שולח הודעה). כיצד ניתן לקבל הודעות אקטיביות דרך מעגל חד-כיווני?

**הפתרון שנבחר:** מנגנון poll — חוט רקע בלקוח שולח בלופ רציף הודעות RELAY ריקות. צומת היציאה מזהה שהמטען ריק ומשתמש ב-`select()` עם timeout קצר (0.5 שניות) כדי לבדוק אם השרת דחף משהו. בכך מושגת תדירות ~2 בדיקות בשנייה — מספיקה לחוויית צ'אט רספונסיבית.

**אתגרים נוספים:**
- **ניהול thread-safety:** שני threads (ה-main thread וחוט הפולינג) יכולים לשלוח RELAY בו-זמנית על אותו socket — נדרש מנגנון lock.
- **הצפנה מקוננת:** בנייה נכונה של שלוש שכבות הצפנה מבפנים החוצה, ופריקה בסדר הנכון בחזרה.
- **framing:** TCP הוא פרוטוקול stream — נדרש header של 4 בתים לכל הודעה כדי לדעת היכן היא מסתיימת.

### הצגת פתרונות שנבחנו

| גישה | יתרונות | חסרונות | הבחירה |
|------|----------|----------|---------|
| RSA-OAEP לאספקת מפתחות | פשוט, חד-כיווני, ללא round-trip נוסף | אין forward secrecy | **נבחר** |
| Diffie-Hellman (כמו Tor האמיתי) | Forward secrecy מלא | דורש round-trip לכל צומת בשלב ה-setup | לא נבחר |
| Long-polling HTTP | מוכר ופשוט | לא מתאים לרשת raw TCP | לא נבחר |
| WebSocket | דו-כיווני | מצריך שכבת HTTP | לא נבחר |

---

## 2. הגדרות

**ניתוב בצל (Onion Routing):** שיטת תקשורת בה ההודעה מוצפנת במספר שכבות. כל צומת ברשת מקלף שכבה אחת ורואה רק את הצומת הבא — לא את השולח המקורי ולא את היעד הסופי.

**מעגל (Circuit):** הנתיב הוירטואלי שנבנה בין הלקוח לשרת היעד דרך שלושה צמתים (Entry → Middle → Exit). המעגל נבנה פעם אחת ושימושי לכל ההודעות באותה הפעלה.

**AES-CBC (Advanced Encryption Standard, Cipher Block Chaining):** אלגוריתם הצפנה סימטרי בגודל בלוק 128 ביט. מצב CBC משתמש ב-IV (Initialization Vector) אקראי לכל הצפנה כך שאותו טקסט גולמי מוצפן לצופן שונה בכל פעם.

**RSA-OAEP (Rivest–Shamir–Adleman, Optimal Asymmetric Encryption Padding):** אלגוריתם הצפנה אסימטרי. משתמשים בו כאן להצפנת מפתח ה-AES האפמרי (16 בתים) עבור כל צומת — רק הצומת שמחזיק את המפתח הפרטי יכול לפענח.

**Hybrid Encryption (הצפנה היברידית):** שילוב RSA + AES: RSA מצפין מפתח AES קטן (16 בתים), ו-AES מצפין את הנתונים בפועל (שיכולים להיות גדולים). פותר את בעיית מגבלת הגודל של RSA (~214 בתים לפני padding).

**PKCS7 Padding:** שיטת padding שמרחיבה את המסר לכפולה של גודל הבלוק (16 בתים ב-AES). לדוגמה, מסר ריק (`b''`) יורחב ל-16 בתים.

**Length-Prefixed Framing:** כל הודעה ב-TCP מקודמת ב-4 בתים (big-endian) המציינים את אורך המטען. כך ניתן לדעת היכן מסתיימת הודעה אחת ומתחילה הבאה.

**poll mechanism:** מנגנון שבו הלקוח שולח "הודעת בדיקה" ריקה בלופ קבוע, וצומת היציאה בודק אם השרת דחף נתונים חדשים. מממש תקשורת דחויה (push) על גבי מעגל חד-כיווני.

**Directory Server (שרת ספרייה):** שרת מרכזי שמחזיק את רשימת הצמתים הפעילים ברשת. הצמתים נרשמים אליו בהפעלה, והלקוחות שואלים אותו כדי לגלות דרך אילו צמתים לבנות מעגל.

**CustomTkinter:** ספרייה לבניית ממשקי משתמש גרפיים (GUI) בפייתון, המרחיבה את Tkinter הסטנדרטי עם עיצוב מודרני.

**pycryptodome:** ספריית הצפנה בפייתון המספקת AES, RSA ואלגוריתמים נוספים.

---

## 3. מבנה / ארכיטקטורה של הפרויקט

### 3.1 הפתרון המוצע והסיבות לבחירתו

הפרויקט מממש רשת ניתוב בצל מינימלית עם שלושה צמתים (entry, middle, exit) ושרת צ'אט. הבחירה בשלושה צמתים בדיוק נובעת מהתפשרות בין אנונימיות לביצועים: שלושה צמתים מספיקים כדי שאף אחד מהצמתים לא ידע גם מי השולח וגם מי הנמען, ועדיין לא מאטים יתר על המידה.

### 3.2 רכיבי המערכת

```
┌─────────────────────────────────────────────────────────────┐
│                     לקוח GUI (CustomTkinter)                │
│  client/main.py  ←→  home_screen.py  ←→  chat_screen.py    │
│                       client/network.py                     │
└──────────────────────────┬──────────────────────────────────┘
                           │  TCP (CIRCUIT_SETUP / RELAY)
                           ▼
┌──────────────────────────────────────────────────────────────┐
│              שרת ספרייה  (directory_server.py)               │
│               פורט 8000  —  רשימת הצמתים הפעילים            │
└────────────────┬─────────────────────────────────────────────┘
                 │ REGISTER / GET_NODES
    ┌────────────┼────────────────────┐
    ▼            ▼                    ▼
┌────────┐  ┌────────┐          ┌────────┐
│ Entry  │  │ Middle │          │  Exit  │
│ Node   │→ │ Node   │ ──────→  │  Node  │
│ :9001  │  │ :9002  │          │ :9003  │
└────────┘  └────────┘          └────┬───┘
                                     │  TCP plaintext
                                     ▼
                              ┌─────────────┐
                              │ Chat Server  │
                              │    :8001    │
                              └─────────────┘
```

| רכיב | קובץ | פורט | תפקיד |
|------|------|------|-------|
| שרת ספרייה | `Servers/directory_server.py` | 8000 | מרשם צמתים, מחלק רשימת צמתים ללקוחות |
| צומת Entry | `Servers/node.py --type entry` | 9001 | מקבל חיבורים מלקוחות, מסיר שכבת K1 |
| צומת Middle | `Servers/node.py --type middle` | 9002 | ממסר, מסיר שכבת K2 |
| צומת Exit | `Servers/node.py --type exit` | 9003 | מסיר שכבת K3, מתקשר עם שרת הצ'אט |
| שרת צ'אט | `Servers/chat_server.py` | 8001 | ניהול חדרים ומשתמשים, שמירת סטטיסטיקות |
| לקוח GUI | `client/main.py` | — | ממשק משתמש גרפי |

### 3.3 זרימת המידע — בניית מעגל (CIRCUIT_SETUP)

הלקוח בונה את המטעינות **מבפנים החוצה** (exit → middle → entry):

```
1. מייצר מפתחות: K1, K2, K3 (16 בתים כל אחד)
2. בונה מטען ל-exit:   RSA-OAEP(exit_pub, setup_key) + AES(setup_key, {K3, dest_host:port})
3. בונה מטען ל-middle: RSA-OAEP(mid_pub, setup_key)  + AES(setup_key, {K2, next:exit, payload_exit})
4. בונה מטען ל-entry:  RSA-OAEP(ent_pub, setup_key)  + AES(setup_key, {K1, next:middle, payload_mid})
5. שולח CIRCUIT_SETUP לצומת Entry
```

```
לקוח ──CIRCUIT_SETUP──► Entry ──CIRCUIT_SETUP──► Middle ──CIRCUIT_SETUP──► Exit
                                                                              │
                                                          "ok" ◄─────────────┘
                         "ok" ◄───────────────────────────────
     "ok" ◄─────────────────
```

#### הרכב החבילות בפירוט — CIRCUIT_SETUP

כל הודעת CIRCUIT_SETUP מוסגרת ב-4 בתים (big-endian) המציינים את אורכה, ולאחר מכן JSON. זהו פורמט ה-framing האחיד של כל הפרוטוקול.

**מבנה החבילה הכוללת שהלקוח שולח לצומת Entry:**

```JSON
┌──────────────────────────────────────────────────────────────────┐
│  4 בתים big-endian  │  JSON payload                             │
│  (אורך ה-JSON)      │                                            │
└──────────────────────────────────────────────────────────────────┘

4 byte big endian (json length)
{
    "type":       "CIRCUIT_SETUP",
    "circuit_id": "<UUID — נוצר על ידי הלקוח, ייחודי לקישור לקוח→Entry>",
    "payload": {
        "encrypted_key":  "<base64 — RSA-OAEP(entry_pub, setup_key_ent)>",
        "encrypted_data": "<base64 — AES-CBC(setup_key_ent, json_inner_entry)>"
    }
}
```

**`json_inner_entry` — התוכן שרק צומת Entry יכול לפענח (לאחר AES-CBC decrypt):**

```json
{
    "key":             "<base64 של K1 — 16 בתים, מפתח RELAY של Entry>",
    "next_host":       "127.0.0.1",
    "next_port":       9002,
    "forward_payload": {
        "encrypted_key":  "<base64 — RSA-OAEP(middle_pub, setup_key_mid)>",
        "encrypted_data": "<base64 — AES-CBC(setup_key_mid, json_inner_middle)>"
    }
}
```

**`json_inner_middle` — התוכן שרק Middle יכול לפענח:**

```json
{
    "key":             "<base64 של K2 — 16 בתים, מפתח RELAY של Middle>",
    "next_host":       "127.0.0.1",
    "next_port":       9003,
    "forward_payload": {
        "encrypted_key":  "<base64 — RSA-OAEP(exit_pub, setup_key_exit)>",
        "encrypted_data": "<base64 — AES-CBC(setup_key_exit, json_inner_exit)>"
    }
}
```

**`json_inner_exit` — התוכן שרק Exit יכול לפענח:**

```json
{
    "key":       "<base64 של K3 — 16 בתים, מפתח RELAY של Exit>",
    "dest_host": "127.0.0.1",
    "dest_port": 8001
}
```

#### מבנה שכבת ה-Hybrid Encryption

כל `payload` (ל-entry, middle ו-exit) נבנה באותו אופן:

```
setup_key  ─── 16 בתים אקראיים (נוצרים ע"י הלקוח, פעם אחת לכל צומת)
               │
               ├─► RSA-OAEP encrypt (עם המפתח הציבורי של הצומת)
               │   → encrypted_key  (256 בתים עבור RSA-2048, מקודד base64)
               │
               └─► AES-128-CBC encrypt (IV אקראי 16B מוסף בראש)
                   → encrypted_data (מקודד base64)
                   מכיל: IV (16B) + ciphertext (כפולה של 16B) + PKCS7 padding
```

**למה Hybrid Encryption?** RSA-2048 יכול להצפין לכל היותר ~214 בתים. ה-`json_inner` של entry כולל גם את כל מטעין ה-middle ובתוכו את מטעין ה-exit — עשרות בתים ויותר. לכן רק ה-`setup_key` (16 בתים) עובר דרך RSA; כל שאר הנתונים מוצפנים ב-AES.

#### cascade בניית המעגל — פירוט מלא

```
שלב 1: הלקוח שולח [4B len][CIRCUIT_SETUP JSON] לצומת Entry
        circuit_id = UUID_A (קישור לקוח↔Entry)

שלב 2: Entry מקבל, מפענח (RSA+AES), מחלץ K1 ואת forward_payload
        Entry פותח TCP ל-Middleמ
        Entry מייצר UUID_B (circuit_id חדש לקישור Entry↔Middle)
        Entry שולח [4B len][CIRCUIT_SETUP JSON] ל-Middle
        circuit_id = UUID_B, payload = forward_payload (אטום ל-Entry)
        Entry חוסם — מחכה ל-{"status": "ok"} מ-Middle

שלב 3: Middle מקבל, מפענח, מחלץ K2 ואת forward_payload
        Middle פותח TCP ל-Exit
        Middle מייצר UUID_C (circuit_id חדש לקישור Middle↔Exit)
        Middle שולח [4B len][CIRCUIT_SETUP JSON] ל-Exit
        circuit_id = UUID_C, payload = forward_payload (אטום ל-Middle)
        Middle חוסם — מחכה ל-{"status": "ok"} מ-Exit

שלב 4: Exit מקבל, מפענח, מחלץ K3 ו-dest_host:port
        Exit פותח TCP מתמשך לשרת הצ'אט (dest_sock — נשאר פתוח כל חיי המעגל)
        Exit מחזיר {"status": "ok"} ל-Middle

שלב 5: Middle מקבל "ok", שומר circuit (K2, next_sock, UUID_C)
        Middle מחזיר {"status": "ok"} ל-Entry

שלב 6: Entry מקבל "ok", שומר circuit (K1, next_sock, UUID_B)
        Entry מחזיר {"status": "ok"} ללקוח

שלב 7: הלקוח מקבל "ok" — המעגל חי
        הלקוח מחזיק: K1, K2, K3, UUID_A, socket ל-Entry
```

**מה כל צומת שומר לאחר ה-setup:**

| צומת | מה הוא יודע |
|------|-------------|
| Entry | K1, UUID_A (נכנס), UUID_B (יוצא), socket ל-Middle |
| Middle | K2, UUID_B (נכנס), UUID_C (יוצא), socket ל-Exit |
| Exit | K3, UUID_C (נכנס), socket לשרת הצ'אט |
| לקוח | K1+K2+K3, UUID_A, socket ל-Entry |

**הערה:** שלושת ה-`setup_key` האפמריים (אחד לכל צומת) נזרקים מיד לאחר השלמת ה-setup ואינם נשמרים בוcרנקשום מקום. RSA לא נמצא בשימוש שוב לאחר שלב זה.

### 3.4 זרימת המידע — שליחת הודעה (RELAY)

```
הלקוח מצפין:  AES_K1( AES_K2( AES_K3(payload) ) )
              ↓
Entry מקלף K1 → מעביר AES_K2(AES_K3(payload)) ל-Middle  (circuit_id = ID_B)
Middle מקלף K2 → מעביר AES_K3(payload) ל-Exit             (circuit_id = ID_C)
Exit מקלף K3 → שולח payload (plaintext) לשרת הצ'אט

שרת הצ'אט מחזיר Ack
Exit מעטף ב-K3, מחזיר RELAY_RESPONSE ל-Middle
Middle מעטף ב-K2, מחזיר ל-Entry
Entry מעטף ב-K1, מחזיר ללקוח
הלקוח מקלף K1 → K2 → K3 → Ack מקורי
```

**הערת אנונימיות:** כל קישור ברשת נושא `circuit_id` שונה (ID_A על הקישור לקוח→Entry, ID_B על Entry→Middle, ID_C על Middle→Exit). Entry מייצרת UUID חדש (`next_circuit_id`) לפני שהיא מעבירה את ה-RELAY קדימה, וכך גם Middle. משקיף שמנטר שני קישורים שונים אינו יכול לקשר ביניהם לפי `circuit_id`.

### 3.5 זרימת המידע — קבלת הודעות (Poll)

```
חוט receiver:
  loop:
    שולח RELAY עם מטען ריק (b'')
    ↓
    Exit: AES_K3_decrypt(b'') → b'' → poll mode
    select(dest_sock, timeout=0.5s)
    אם יש נתונים: recv_msg(dest_sock) → שרת הצ'אט דחף הודעה
    אחרת: raw_response = b''
    ↓
    Exit מחזיר RELAY_RESPONSE (מוצפן K3)
    Middle, Entry מוסיפים שכבות
    ↓
    הלקוח מקלף → אם ריק: None (המשך loop) | אחרת: הודעה חדשה
```

### 3.6 פרוטוקול הצ'אט

**מבנה Frame:**
```
┌─────────────────────┬──────────────────────────────┐
│  4 בתים big-endian  │  מטען JSON                   │
│  (אורך המטען)       │  {"type": "...", "data": {}} │
└─────────────────────┴──────────────────────────────┘
```

**הודעות מהלקוח לשרת:**

| type | data | תיאור |
|------|------|-------|
| `CreateRoom` | `{"my_username": str}` | יצירת חדר חדש |
| `JoinRoom` | `{"room_code": str, "my_username": str}` | הצטרפות לחדר |
| `LeaveRoom` | `{}` | עזיבת חדר |
| `SendMessage` | `{"message": str}` | שליחת הודעת טקסט |
| `SendFile` | `{"filename": str, "filedata": str (base64)}` | שליחת קובץ |
| `GetStats` | `{}` | בקשת סטטיסטיקות |

**הודעות מהשרת ללקוח:**

| type | data | תיאור |
|------|------|-------|
| `RoomCreated` | `{"room_code": str, "users": [...]}` | חדר נוצר |
| `RoomJoined` | `{"room_code": str, "users": [...]}` | הצטרפות אושרה |
| `RoomLeft` | `{"room_code": str}` | עזיבה אושרה |
| `IncomingMessage` | `{"from_username": str, "message": str}` | הודעה נכנסת |
| `IncomingFile` | `{"from_username": str, "filename": str, "filedata": str}` | קובץ נכנס |
| `UserJoined` | `{"username": str, "room_code": str}` | משתמש חדש הצטרף |
| `UserLeft` | `{"username": str, "room_code": str}` | משתמש עזב |
| `Stats` | `{"total_messages": int, "total_files": int, "total_users": int, "total_rooms": int}` | סטטיסטיקות |
| `Ack` | `{}` | אישור קבלה (נדרש למנגנון poll) |
| `Error` | `{"error_message": str}` | שגיאה |

### 3.7 פרוטוקול הניתוב — פירוט מלא של הודעות וחבילות

#### סוגי הודעות

| type | כיוון | תיאור | תגובה |
|------|-------|-------|-------|
| `CIRCUIT_SETUP` | לקוח → entry → middle → exit | בניית מעגל | `{"status": "ok"}` |
| `RELAY` | לקוח → entry → middle → exit | שליחת מטען מוצפן | `RELAY_RESPONSE` |
| `RELAY_RESPONSE` | exit → middle → entry → לקוח | תגובה מוצפנת בחזרה | — |
| `REGISTER` | צומת → directory | רישום צומת | `{"status": "ok"}` |
| `GET_NODES` | לקוח → directory | בקשת רשימת צמתים | `{"nodes": {"entry": […], "middle": […], "exit": […]}}` — כל ערך הוא רשימת dicts עם `host`, `port`, `public_key` |
| `PING` | directory → צומת | בדיקת תקינות | `{"status": "pong"}` |

#### Framing — מסגרת כל הודעה

```
┌──────────────────────┬────────────────────────────────────────┐
│  4 בתים big-endian   │  payload (JSON כ-UTF-8)                │
│  = אורך ה-payload    │                                        │
└──────────────────────┴────────────────────────────────────────┘
```

כלל זה חל על **כל** הודעה בפרוטוקול: CIRCUIT_SETUP, RELAY, RELAY_RESPONSE, REGISTER, GET_NODES, וכן פרוטוקול הצ'אט. `send_msg` / `recv_msg` מיישמות framing זה.

#### הרכב חבילת RELAY

הלקוח מצפין שלוש שכבות מבפנים החוצה, ואז שולח:

```
[4B len][JSON]

JSON:
{
    "type":       "RELAY",
    "circuit_id": "<UUID_A — קישור לקוח↔Entry>",
    "data":       "<base64 של AES_K1( AES_K2( AES_K3(payload) ) )>"
}
```

**מבנה שדה `data` (לאחר base64-decode):**

```
AES-CBC שכבה 1 (K1, IV_1):
┌──────────────────────────────────────────────────────────────────┐
│  IV_1 (16B)  │  AES-CBC(K1, IV_1,  ←── תוכן שכבה 1               │
│              │    IV_2 (16B) + AES-CBC(K2, IV_2,                 │
│              │      IV_3 (16B) + AES-CBC(K3, IV_3, payload)      │
│              │    )                                              │
│              │  )                                                │
└──────────────────────────────────────────────────────────────────┘
```

**פירוק לשכבות:**

| שכבה | מפתח | IV | תוכן לאחר פיענוח |
|------|------|----|------------------|
| חיצונית (Entry) | K1 | IV_1 (16B ראשונים) | `IV_2 + AES_K2(AES_K3(payload))` |
| אמצעית (Middle) | K2 | IV_2 (16B ראשונים) | `IV_3 + AES_K3(payload)` |
| פנימית (Exit) | K3 | IV_3 (16B ראשונים) | `payload` (plaintext של הצ'אט) |

כאשר `payload` ב-RELAY ריק (`b''`), הוא מצפין ל-32 בתים (IV + בלוק PKCS7 אחד) — בלתי ניתן להבחין מ-RELAY אמיתי.

**עיבוד בכל צומת:**

```python
Entry מקבל RELAY עם circuit_id=UUID_A:
  blob  = base64_decode(msg["data"])          # = AES_K1(AES_K2(AES_K3(p)))
  peeled = aes_decrypt(K1, blob)              # = AES_K2(AES_K3(p))
  שולח RELAY עם circuit_id=UUID_B, data=base64(peeled) ל-Middle
  חוסם — מחכה ל-RELAY_RESPONSE מ-Middle

Middle מקבל RELAY עם circuit_id=UUID_B:
  blob  = base64_decode(msg["data"])          # = AES_K2(AES_K3(p))
  peeled = aes_decrypt(K2, blob)              # = AES_K3(p)
  שולח RELAY עם circuit_id=UUID_C, data=base64(peeled) ל-Exit
  חוסם

Exit מקבל RELAY עם circuit_id=UUID_C:
  blob  = base64_decode(msg["data"])          # = AES_K3(p)
  plaintext = aes_decrypt(K3, blob)           # = p (payload הצ'אט)
  אם plaintext לא ריק → send_msg(dest_sock, plaintext), select(5s)
  אם ריק (poll)        → select(dest_sock, 0.5s)
  raw_response = תגובת השרת (או b'' אם timeout)
  re_enc = aes_encrypt(K3, raw_response)
  שולח RELAY_RESPONSE לאחור
```

#### הרכב חבילת RELAY_RESPONSE

```json
[4B len][JSON]

JSON (correct return circuit):
{
    "type":       "RELAY_RESPONSE",
    "circuit_id": "<UUID of the circuit>",
    "data":       "<base64 of encrypted data>"
}

JSON (Error):
{
    "type":       "RELAY_RESPONSE",
    "circuit_id": "<UUID>",
    "error":      "<error description>"
}
```

**ערמת ההצפנה בדרך חזרה:**

```
Exit   שולח: base64( AES_K3(raw_response) )
Middle שולח: base64( AES_K2( AES_K3(raw_response) ) )
Entry  שולח: base64( AES_K1( AES_K2( AES_K3(raw_response) ) ) )

הלקוח מפענח:
  step1 = aes_decrypt(K1, data)   →  AES_K2(AES_K3(raw_response))
  step2 = aes_decrypt(K2, step1)  →  AES_K3(raw_response)
  step3 = aes_decrypt(K3, step2)  →  raw_response (bytes with the original chat protocol)
```

אם `raw_response == b''` — אין הודעה ממתינה (poll החזיר ריק).

#### הרכב חבילת RELAY ריק — מנגנון Poll

הלקוח מצפין `b''` (בתים ריקים) בשלוש שכבות:

```python
AES-CBC(K3, b'')  →  IV_3 (16B) + PKCS7_block (16B)  =  32 בתים
AES-CBC(K2, ↑)   →  IV_2 (16B) + ciphertext (32B)    =  48 בתים
AES-CBC(K1, ↑)   →  IV_1 (16B) + ciphertext (48B)    =  64 בתים

שדה "data" ב-RELAY: base64(64 בתים) = ~88 תווים
```

Exit מזהה poll: `aes_decrypt(K3, blob) == b''` → מפעיל `select(dest_sock, 0.5s)` ללא שליחה.

#### הרכב חבילות שרת הספרייה

**REGISTER (צומת → directory):**
```json
[4B len]
{
    "type":       "REGISTER",
    "node_type":  "entry" | "middle" | "exit",
    "host":       "<directory ip>",
    "port":       <directory port>,
    "public_key": "-----BEGIN RSA PUBLIC KEY-----\n...\n-----END RSA PUBLIC KEY-----"
}
```

**GET_NODES (לקוח → directory):**
```json
[4B len]
{"type": "GET_NODES"}
```

**תגובת directory ללקוח:**
```json
[4B len]
{
  "nodes": {
      "entry":  [{"host": "<node ip>", "port": <node port>, "public_key": "...", "node_type": "entry"}],
      "middle": [{"host": "<node ip>", "port": <node port>, "public_key": "...", "node_type": "middle"}],
      "exit":   [{"host": "<node ip>", "port": <node port>, "public_key": "...", "node_type": "exit"}]
  }
}
```

הצמתים מקובצים לפי סוג ישירות בשרת הספרייה — `pick_nodes()` בצד הלקוח לא נדרשת לקבץ בעצמה.

**PING / pong (health-check):**
```json
[4B len]{"type": "PING"}   // directory -> node
[4B len]{"status": "pong"} // node -> directory
```

### 3.8 אבטחה

- **הצפנה בשכבות:** כל הודעה מוצפנת שלוש פעמים. כל צומת מכיר רק את המפתח שלו.
- **מה Entry יודע:** IP המקור (הלקוח), כתובת Middle — לא יודע מה תוכן ההודעה ולא מי היעד.
- **מה Middle יודע:** כתובת Entry, כתובת Exit — לא יודע מי השולח ולא מי היעד.
- **מה Exit יודע:** כתובת Middle, שרת הצ'אט — לא יודע מי השולח המקורי.
- **שרת הצ'אט:** רואה רק את IP של צומת Exit, לא את IP הלקוח האמיתי.
- **IV אקראי:** כל הצפנת AES משתמשת ב-IV חדש — מונע ניתוח של ciphertext זהה.

### 3.9 טכנולוגיות בשימוש

| טכנולוגיה | שימוש |
|-----------|-------|
| Python 3.14+ | שפת הפיתוח הבלעדית |
| UV | ניהול חבילות ו-venv |
| pycryptodome | AES-CBC, RSA-2048-OAEP |
| customtkinter | ממשק גרפי מודרני |
| sqlite3 | שמירת סטטיסטיקות (מובנה ב-Python) |
| threading | ניהול threads מקביליים |
| socket | תקשורת TCP |
| select | I/O non-blocking בצמתים |
| uuid | יצירת מזהי מעגלים |

---

## 4. מדריך למשתמש

### 4.1 דרישות מערכת

- Python 3.14 ומעלה
- [UV](https://docs.astral.sh/uv/) — מנהל חבילות ו-venv מודרני לפייתון
- מערכת הפעלה: Windows / Linux / macOS
- ספריות: `pycryptodome`, `customtkinter` (מנוהלות דרך `pyproject.toml`)

### 4.2 התקנה

```bash
# clone the repo
git clone <repo-url>
cd school-tor

# install dependencies (managed via UV)
uv sync
```

### 4.3 הפעלה מהירה — סקריפט אוטומטי

```bash
# Windows
start.bat

# macOS
./start.sh

# עם אפשרויות
start.bat --nodes 3 --debug
./start.sh --nodes 2

# הפעלת הלקוח לאחר מכן
uv run client/main.py
```

הסקריפטים מפעילים בסדר: שרת ספרייה → שרת צ'אט → צמתים → לקוח GUI.

### 4.4 הפעלה ידנית (שבע חלוניות טרמינל)

```bash
# terminal 1: directory server
uv Servers/directory_server.py

# terminal 2: chat server
uv Servers/chat_server.py --port 8001

# terminal 3: Entry node
uv Servers/node.py --type entry --port 9001

# terminal 4: Middle node
uv Servers/node.py --type middle --port 9002

# terminal 5: Exit node
uv Servers/node.py --type exit --port 9003

# terminal 6: GUI client
uv client/main.py
```

### 4.5 ממשק המשתמש — תרשים מסכים

```
┌─────────────────────┐
│    מסך הבית         │
│  (Home Screen)      │
│                     │
│  [Connect]          │
│  [Create Room] ─────┼──→ מסך הצ'אט
│  [Join Room]   ─────┼──→ (Chat Screen)
└─────────────────────┘
```

### 4.6 מסך הבית (Home Screen)

```
┌──────────────────────────────────────────────────────────────┐
│  Onion Chat  Server:[127.0.0.1]  Port:[8001]  [ ]Tor  [Connect]  ● Disconnected │
├────────────────────────┬─────────────────────────────────────┤
│  Server Statistics     │  Create Room                         │
│  ─────────────────     │  Username: [___________]             │
│  Messages       42     │  [Create Room]                       │
│  Files Sent      3     ├─────────────────────────────────────┤
│  Total Users    15     │  Join Room                           │
│  Total Rooms     2     │  Username: [___________]             │
│  ─────────────────     │  Room Code: [____________________]   │
│  [Refresh Now]         │  [Join Room]                         │
└────────────────────────┴─────────────────────────────────────┘
│  Fill in connection settings and click Connect.               │
└──────────────────────────────────────────────────────────────┘
```

**אלמנטים במסך הבית:**

| אלמנט | תפקיד |
|-------|-------|
| שדה Server | כתובת IP של שרת הצ'אט (ברירת מחדל: `127.0.0.1`) |
| שדה Port | פורט שרת הצ'אט (ברירת מחדל: `8001`) |
| תיבת סימון Route via Tor | כאשר מסומנת, מגלה שדות Dir Host ו-Dir Port |
| שדה Dir Host | כתובת שרת הספרייה (נראה רק במצב Tor) |
| שדה Dir Port | פורט שרת הספרייה (ברירת מחדל: `8000`) |
| כפתור Connect | פותח חיבור לשרת (ישיר או דרך Tor) |
| אינדיקטור ● | ירוק = מחובר, אדום = מנותק |
| לוח סטטיסטיקות | מרענן אוטומטית כל 5 שניות |
| כפתור Refresh Now | ריענון ידני מיידי של הסטטיסטיקות |
| שדה Username (Create) | שם המשתמש ליצירת חדר |
| כפתור Create Room | יוצר חדר חדש ומעביר למסך הצ'אט |
| שדה Username (Join) | שם המשתמש להצטרפות |
| שדה Room Code | קוד UUID של חדר קיים |
| כפתור Join Room | מצטרף לחדר קיים |
| שורת סטטוס (תחתית) | הודעות מידע/שגיאות חולפות |

**הגבלות:**
- שם משתמש לא יכול להיות ריק
- קוד חדר לא יכול להיות ריק
- הכפתורים Create/Join/Refresh מושבתים כל עוד אין חיבור פעיל

### 4.7 מסך הצ'אט (Chat Screen)

```
┌───────────────────────────────────────────────────────────────┐
│  Room: abc-123-...  [Copy Code]                  [Leave Room] │
├────────────────────────────────────┬──────────────────────────┤
│  הודעות (גלילה)                    │  משתמשים                 │
│                                    │  ─────────────────        │
│  you: שלום!                        │    alice                  │
│  alice: היי!                       │    bob                    │
│  *** bob הצטרף לחדר ***            │    carol                  │
│  bob: מה קורה                      │                           │
│  alice שלח: doc.pdf (~42 KB) [שמור]│                           │
│  *** carol עזב את החדר ***         │                           │
│                                    │                           │
├────────────────────────────────────┴──────────────────────────┤
│  [צרף קובץ]  [הקלד הודעה...]                         [שלח]   │
└───────────────────────────────────────────────────────────────┘
```

**אלמנטים במסך הצ'אט:**

| אלמנט | תפקיד |
|-------|-------|
| כפתור Copy Code | מעתיק את קוד החדר ללוח — לשיתוף עם אחרים |
| כפתור Leave Room | עוזב את החדר ומחזיר למסך הבית |
| תיבת הודעות | תצוגת ההיסטוריה (read-only) |
| רשימת משתמשים | עדכון חי של מי בחדר |
| כפתור צרף קובץ | פותח דיאלוג בחירת קובץ |
| שדה כתיבת הודעה | מחרוזת ההודעה לשליחה |
| כפתור שלח | שולח את ההודעה |
| כפתור שמור (בהודעת קובץ) | שומר קובץ שהתקבל לדיסק |

**צבעי הטקסט בתיבת ההודעות:**

| צבע | משמעות |
|-----|---------|
| ירוק | הודעות שלך |
| כחול | הודעות משתמשים אחרים |
| אפור נטוי | הודעות מערכת (הצטרפות/עזיבה) |
| ענבר | הודעות העברת קובץ |
| אדום | שגיאות |

### 4.8 שליחת קובץ

1. לחץ על **[צרף קובץ]** בתחתית המסך
2. בחר קובץ בדיאלוג
3. הקובץ נשלח אוטומטית לכל המשתמשים בחדר
4. מקבלים רואים: `username שלח: filename (~XX KB) [שמור]`
5. לחץ **[שמור]** לשמירת הקובץ — יפתח דיאלוג לבחירת נתיב

### 4.9 הודעות מצב למשתמש

| הודעה | משמעות |
|-------|---------|
| "Fill in connection settings and click Connect." | לא מחובר — יש למלא פרטים ולחצו Connect |
| "Connecting…" | מתבצע חיבור |
| "[Tor] Querying directory…" | שואל את שרת הספרייה |
| "[Tor] Building circuit…" | בונה מעגל Tor |
| "Connected (Direct). Stats refresh every 5 s." | מחובר ישירות |
| "Connected (Tor). Stats refresh every 5 s." | מחובר דרך Tor |
| "Settings changed — reconnect to apply" | הגדרות השתנו — יש להתחבר מחדש |
| "Connection failed: …" | חיבור נכשל (עם פרטי שגיאה) |
| "Not connected — click Connect first." | ניסיון יצירת/הצטרפות ללא חיבור |

---

## 5. מדריך למפתח

### 5.1 מבנה תיקיות הפרויקט

```bash
school-tor/
├── Servers/
│   ├── directory_server.py   # directory server
│   ├── node.py               # tor node (all 3 types)
│   └── chat_server.py        # chat server
├── client/
│   ├── main.py               # App class and entry point for client
│   ├── network.py            # network layer — TorSocket, Connection
│   ├── home_screen.py        # home screen
│   └── chat_screen.py        # chat screen
├── start.bat                 # quick server launch script for Windows
├── start.sh                  # quick server launch script for macOS
├── pyproject.toml            # Python dependencies (UV)
├── uv.lock                   # UV lock file
└── requirements.txt          # legacy dependencies, unused
```

### 5.2 `Servers/directory_server.py` — שרת ספרייה

**תפקיד:** שרת מרכזי לניהול רשימת הצמתים. צמתים נרשמים כאן בהפעלה; לקוחות שואלים כאן לפני בניית מעגל.

**מיקום:** `Servers/directory_server.py`

**משתנים גלובליים משמעותיים:**

| משתנה | סוג | תפקיד |
|-------|-----|-------|
| `nodes` | `list[dict]` | רשימת כל הצמתים הרשומים |
| `nodes_lock` | `threading.Lock` | mutex לגישה בטוחה ל-`nodes` מ-threads שונים |
| `_CHECK_INTERVAL` | `int` = 10 | מרווח בשניות בין סבבי health-check |
| `_MAX_FAILURES` | `int` = 3 | מספר כשלים עד הסרת צומת |

**פורמט entry ברשימת הצמתים:**
```python
{
    'node_type':  'entry' | 'middle' | 'exit',
    'host':       str,
    'port':       int,
    'public_key': str,   # PEM of RSA public key
    'fail_count': int,   # current number of health check fails
}
```

**פונקציות:**

---

**`recv_msg(sock) → bytes | None`**
קוראת הודעה אחת מלאה מ-socket. קוראת 4 בתים header לידיעת האורך, ואז קוראת בלולאה עד לקריאת כל הבתים. מחזירה `None` אם ה-socket נסגר.

---

**`send_msg(sock, data)`**

שולחת נתונים עם prefix של 4 בתים big-endian. מקבלת `dict` (ממיר ל-JSON), `str` (ממיר ל-UTF-8), או `bytes`. משתמשת ב-`sendall()` להבטחת שליחה מלאה.

---

**`check_node(node: dict) → bool`**

פותחת TCP חיבור קצר לצומת, שולחת `{"type": "PING"}`, ומצפה לתשובה `{"status": "pong"}` תוך 5 שניות. מחזירה `True` אם הצומת חי.

---

**`health_check_loop()`**

thread daemon שרץ ברקע. כל `_CHECK_INTERVAL` שניות — עובר על כל הצמתים ומפעיל `check_node()`. בהצלחה: מאפס `fail_count`. בכשל: מגדיל ב-1. אם `fail_count >= _MAX_FAILURES`: מסיר את הצומת.

---

**`handle_client(conn, addr)`**

מטפלת בחיבור אחד: קוראת הודעה אחת ומגיבה.
- `REGISTER`: מוחקת entry ישנה (אותו host:port), מוסיפה entry חדשה.
- `GET_NODES`: מחזירה את רשימת הצמתים (ללא `fail_count`).

---

**`main()`**

נקודת כניסה: bind לפורט 8000, מפעיל חוט ה-health-check, לולאת accept עם timeout של שנייה אחת לטיפול ב-Ctrl+C.

---

### 5.3 `Servers/node.py` — צומת ממסר

**תפקיד:** מממש שלושת סוגי הצמתים (entry/middle/exit) בקובץ יחיד. כל instance מופעל עם `--type`.

**מיקום:** `Servers/node.py`

**משתנים גלובליים:**

| משתנה | סוג | תפקיד |
|-------|-----|-------|
| `rsa_key` | `RSA.RsaKey` | מפתח RSA-2048 פרטי של הצומת (נוצר בהפעלה) |
| `node_type` | `str` | `'entry'`, `'middle'` או `'exit'` |
| `debug` | `bool` | מצב debug — מדפיס hex preview של הודעות |
| `circuits` | `dict[str, dict]` | מילון מעגלים פעילים, ממופה לפי circuit_id |
| `circuits_lock` | `threading.Lock` | mutex לגישה ל-`circuits` |

**פורמט entry במילון `circuits`:**
```python
{
    'key':             bytes,           # AES key of the node (K1/K2/K3)
    'next_sock':       socket | None,   # live socket to next hop (entry/middle)
    'next_circuit_id': str | None,      # UUID for next hop (entry/middle)
    'dest_sock':       socket | None,   # live socket to dest server (exit)
    'dest':            tuple | None,    # (host, port) of the dest (exit)
    'is_exit':         bool,            # is this an exit node?
}
```

**פונקציות:**

---

**`aes_encrypt(key, plaintext) → bytes`**

מצפין AES-128-CBC עם IV אקראי. מחזיר `iv (16B) + ciphertext`. PKCS7 padding. מטען ריק (`b''`) חוקי.

---

**`aes_decrypt(key, ciphertext) → bytes`**

מפענח AES-128-CBC. מחלץ IV מ-16 הבתים הראשונים, מפענח, ומסיר PKCS7 padding.

---

**`dbg(label, data)`**

מדפיס 32 הבתים הראשונים של `data` ב-hex כאשר `debug=True`. לעיבוד אבחון בלבד.

---

**`decrypt_setup_payload(payload: dict) → dict`**

מפענחת מטען CIRCUIT_SETUP הכולל שני שלבים:
1. RSA-OAEP decrypt של `encrypted_key` → `setup_key` (16 בתים).
2. AES-CBC decrypt של `encrypted_data` עם `setup_key` → JSON עם נתוני הניתוב.

---

**`handle_circuit_setup(conn, msg, local_circuits: set)`**

מטפלת ב-CIRCUIT_SETUP:
- מפענחת את המטען עם `decrypt_setup_payload()`.
- **exit (יש `dest_host`):** פותחת TCP ל-dest, שומרת circuit, מחזירה `{"status": "ok"}`.
- **entry/middle (יש `next_host`):** פותחת TCP לצומת הבא, **מייצרת `next_circuit_id` חדש (UUID)** לקישור היוצא, מעבירה `CIRCUIT_SETUP` עם `next_circuit_id` ו-`forward_payload`, מחכה ל-`{"status": "ok"}`, שומרת circuit (כולל `next_circuit_id`), מחזירה `{"status": "ok"}`.

---

**`handle_relay(conn, msg)`**

מטפלת ב-RELAY:
1. מחפשת circuit לפי `circuit_id`.
2. מפענחת `data` עם המפתח שלה (קולפת שכבה אחת).
3. **exit:** אם הפלייינטקסט לא ריק → שולחת לשרת הצ'אט, מחכה 5 שניות. אם ריק → poll, מחכה 0.5 שניות. מצפינה התגובה ב-K ומחזירה RELAY_RESPONSE.
4. **entry/middle:** מעבירה RELAY לצומת הבא עם **`circuit['next_circuit_id']`** (UUID שנוצר בשלב ה-CIRCUIT_SETUP, שונה מ-`circuit_id` הנכנס), מחכה ל-RELAY_RESPONSE, מוסיפה את השכבה שלה, מחזירה RELAY_RESPONSE.

---

**`handle_connection(conn, addr)`**

לולאת קריאה עבור חיבור TCP אחד. מנתבת לפי `type`: `CIRCUIT_SETUP` → `handle_circuit_setup`, `RELAY` → `handle_relay`, `PING` → `{"status": "pong"}`. ב-`finally`: סוגרת את `next_sock` / `dest_sock` לכל מעגל שנפתח בחיבור זה — מה שמפעיל teardown cascade.

---

**`register_with_directory(dir_host, dir_port, n_type, host, port, pub_pem)`**

שולחת `REGISTER` לשרת הספרייה עם פרטי הצומת ומפתח ה-RSA הציבורי. זורקת `RuntimeError` אם הרישום נכשל.

---

**`main()`**

1. מנתחת ארגומנטי CLI: `--type`, `--host`, `--port`, `--dir-host`, `--dir-port`, `--debug`.
2. מייצרת מפתח RSA-2048 (`RSA.generate(2048)`).
3. קוראת ל-`register_with_directory()`.
4. bind + listen, לולאת accept, thread per connection.

---

### 5.4 `Servers/chat_server.py` — שרת הצ'אט

**תפקיד:** שרת TCP רב-חדרי. מנהל חדרים ומשתמשים בזיכרון, שומר סטטיסטיקות ב-SQLite.

**מיקום:** `Servers/chat_server.py`

**ארכיטקטורה (ארבע classes:**

```
ChatServer
  └── ChatMessageHandler (dispatcher חסר state)
        └── UserManager (כל לוגיקת החדרים/משתמשים)
              ├── Room (מאחסן חיבורים של חדר אחד)
              └── Database (SQLite — stats בלבד)
```

---

**Class `Database`**

שומרת סטטיסטיקות מצטברות שישרדו אתחול מחדש.

| מתודה | תפקיד |
|-------|-------|
| `__init__(path)` | פותחת/יוצרת SQLite, מאתחלת schema |
| `_init_schema()` | יוצרת טבלת `stats` ומאכלסת מפתחות אם לא קיימים |
| `increment_stat(*keys)` | מגדיל מונה ב-1 ב-SQL (atomic) |
| `get_stats() → dict` | קוראת את כל המונים מה-DB |

---

**Class `Room`**

מאחסן in-memory את ה-sockets וה-usernames של חדר אחד.

| מתודה | תפקיד |
|-------|-------|
| `add(conn, username)` | הוספת לקוח לחדר |
| `remove(conn)` | הסרת לקוח (בטוחה — מתעלמת ממפתח לא קיים) |
| `get_username(conn) → str\|None` | שליפת שם משתמש לפי socket |
| `others(exclude_conn) → list` | snapshot של כל החיבורים מלבד exclude_conn |
| `is_empty() → bool` | האם החדר ריק? |

---

**Class `UserManager`**

ניהול כל state החדרים/משתמשים.

| מתודה | תפקיד |
|-------|-------|
| `get_room_of(conn) → str\|None` | מוצאת את קוד החדר של conn |
| `get_username_of(conn) → str\|None` | מוצאת שם משתמש של conn |
| `create_room(conn, username) → (code, members)` | יוצרת חדר חדש, מוסיפה את היוצר |
| `join_room(conn, room_code, username) → (members, others)` | מצרפת לחדר, מחזירה רשימת חברים |
| `leave_room(conn, notify_self=True)` | מסירה מחדר, שולחת UserLeft לשאר |
| `broadcast(conn, msg_type, data)` | שולחת הודעה לכולם בחדר מלבד conn |

**עיקרון נעילה:** `UserManager._lock` מגן על state בזיכרון בלבד; ה-DB מנוהל עם lock נפרד. שני ה-locks לא נעולים בו-זמנית.

---

**Class `ChatMessageHandler`**

dispatcher חסר state. מתודה `handle(conn, msg_type, data)` מנתבת ל:

| msg_type | פעולה | תגובה |
|----------|-------|-------|
| `CreateRoom` | `um.create_room()` | `RoomCreated` |
| `JoinRoom` | `um.join_room()` + broadcast `UserJoined` | `RoomJoined` |
| `LeaveRoom` | `um.leave_room()` | `RoomLeft` + broadcast `UserLeft` |
| `SendMessage` | `um.broadcast(IncomingMessage)` + increment | **`Ack`** (חובה לפולינג) |
| `SendFile` | `um.broadcast(IncomingFile)` + increment | **`Ack`** (חובה לפולינג) |
| `GetStats` | `db.get_stats()` | `Stats` |

---

**Class `ChatServer`**

| מתודה | תפקיד |
|-------|-------|
| `__init__(host, port, db_path)` | מאתחלת Database, UserManager, Handler |
| `start()` | bind/listen, לולאת accept, thread per connection |
| `_handle_connection(conn, addr)` | לולאת קריאה, JSON dispatch, `leave_room()` ב-`finally` |

---

### 5.5 `client/network.py` — שכבת הרשת

**תפקיד:** כל לוגיקת הרשת של הלקוח — wire helpers, בניית מעגל Tor, `TorSocket`, `Connection`.

**מיקום:** `client/network.py`

**פונקציות עזר (wire helpers):**

---

**`recv_msg(sock) → bytes | None`**, **`send_msg(sock, data)`**, **`send_to(sock, msg_type, data)`**

זהות לאלו שבשרתים — framing protocol אחיד לכל הרכיבים.

---

**`aes_encrypt(key, plaintext) → bytes`**, **`aes_decrypt(key, ciphertext) → bytes`**

כנ"ל — AES-128-CBC עם IV אקראי.

---

**`get_nodes(dir_host, dir_port) → dict`**

שואלת את שרת הספרייה: שולחת `GET_NODES`, מחזירה dict מקובץ לפי סוג — `{'entry': [...], 'middle': [...], 'exit': [...]}`.

---

**`pick_nodes(nodes) → (entry, middle, exit)`**

בוחרת אקראית צומת אחד מכל סוג. זורקת `RuntimeError` אם סוג חסר.

---

**`make_setup_payload(pub_pem, inner) → dict`**

בונה מטען hybrid-encrypted לצומת אחד:
1. מייצרת `setup_key` (16 בתים אקראי).
2. RSA-OAEP encrypt `setup_key` עם `pub_pem`.
3. AES-CBC encrypt `json(inner)` עם `setup_key`.
4. מחזירה `{encrypted_key: base64, encrypted_data: base64}`.

---

**`build_circuit(entry, middle, exit_node, dest_host, dest_port) → (circuit_id, K1, K2, K3, sock)`**

1. מייצרת `circuit_id` (UUID) ו-K1, K2, K3.
2. בונה `exit_payload`, `middle_payload`, `entry_payload` מבפנים החוצה.
3. מתחברת TCP לצומת Entry.
4. שולחת `CIRCUIT_SETUP`, מחכה ל-`{"status": "ok"}`.
5. מחזירה חמישה ערכים ל-`TorSocket`.

---

**Class `TorSocket`**

drop-in socket replacement שמנתב דרך מעגל Tor.

| מתודה | תפקיד |
|-------|-------|
| `__init__(circuit_id, K1, K2, K3, entry_sock)` | שמירת פרטי המעגל, אתחול `_buf` ו-`_lock` |
| `sendall(data)` | מסיר 4B frame, מצפין K3→K2→K1, שולח RELAY, קורא RELAY_RESPONSE, מפענח K1→K2→K3, מאחסן ב-`_buf` |
| `recv(n) → bytes` | מחזיר n בתים מ-`_buf` (כמו `socket.recv`) |
| `poll() → dict None` | מצפין `b''`, שולח RELAY ריק, מפענח תגובה — None אם ריק, parsed dict אם יש |
| `close()` | סוגר `entry_sock` — מפעיל teardown cascade |

**משתנים חשובים:**

| משתנה | תפקיד |
|-------|-------|
| `_cid` | circuit_id (UUID) |
| `_K1, _K2, _K3` | מפתחות AES לשלושת הצמתים |
| `_entry` | ה-socket האמיתי לצומת Entry |
| `_buf` | buffer של תגובות שהגיעו מהשרת |
| `_lock` | מונע race בין `sendall` (main thread) ו-`poll` (receiver thread) |

---

**Class `Connection`**

עוטפת raw socket (direct) או `TorSocket` (Tor). API אחיד לשני מסכי ה-GUI.

| מתודה | תפקיד |
|-------|-------|
| `send_to(msg_type, data)` | שולחת הודעה מסוג מוגדר |
| `recv_one() → dict  None` | קריאה סינכרונית של הודעה אחת |
| `start_receiver(on_message, on_disconnect)` | מפעיל thread רקע לקבלת הודעות |
| `stop_receiver()` | מפסיק receiver thread (socket נשאר פתוח) |
| `close()` | stop + סגירת socket |
| `is_tor` (property) | `True` אם Tor mode |

**`_direct_receiver`** — משתמש ב-`select()` עם timeout 0.5 שניות.
**`_tor_receiver`** — משתמש ב-`TorSocket.poll()`.
שניהם מסננים `Ack` (לא מעבירים ל-UI).

---

**פונקציות נוחות:**

**`connect_direct(host, port) → Connection`** — יוצרת TCP socket, עוטפת ב-Connection.

**`connect_tor(dir_host, dir_port, dest_host, dest_port) → Connection`** — שואלת directory, בוחרת צמתים, בונה מעגל, עוטפת ב-Connection.

---

### 5.6 `client/main.py` — נקודת כניסה

**תפקיד:** מאתחל חלון CustomTkinter, מחזיק את ה-Connection הפעיל, מנהל מעברי מסכים.

**Class `App(ctk.CTk)`:**

| מתודה | תפקיד |
|-------|-------|
| `__init__()` | גדלי חלון (960×620, min 800×520), רישום `_on_close`, קריאה ל-`show_home()` |
| `show_home(conn=None)` | מחריב מסך נוכחי, מציג HomeScreen עם conn נוכחי |
| `show_chat(conn, username, room_code, members)` | מחריב HomeScreen, מציג ChatScreen |
| `_set_conn(conn)` | callback מ-HomeScreen לשמירת connection חדש |
| `_on_close()` | WM_DELETE_WINDOW — סוגר Connection, מחריב חלון |

**משתנים:**

| משתנה | תפקיד |
|-------|-------|
| `_conn` | ה-Connection הפעיל (None אם לא מחובר) |
| `_frame` | המסך הנוכחי (HomeScreen או ChatScreen) |

---

### 5.7 `client/home_screen.py` — מסך הבית

**תפקיד:** חיבור לשרת, תצוגת סטטיסטיקות, יצירת/הצטרפות לחדר.

**Class `HomeScreen(ctk.CTkFrame)`:**

**build methods:**

| מתודה | תפקיד |
|-------|-------|
| `_build_ui()` | grid 2 עמודות, קורא ל-build sub-methods |
| `_build_topbar()` | שורת כותרת: Server/Port/Tor/Dir/Connect/status |
| `_build_stats_panel()` | לוח שמאלי: 4 מונים + Refresh Now |
| `_build_room_panel()` | לוח ימני: Create Room + Join Room forms |
| `_build_status_bar()` | שורת סטטוס תחתונה |

**connection methods:**

| מתודה | תפקיד |
|-------|-------|
| `_on_tor_toggle()` | מציג/מסתיר שדות Dir Host/Port |
| `_get_params()` | קורא ומאמת שדות חיבור |
| `_manual_connect()` | סוגר חיבור ישן, פותח חדש ב-worker thread |
| `_on_connect_success(conn)` | עדכון UI לאחר חיבור מוצלח |
| `_on_connect_fail(err)` | עדכון UI לאחר כשל |
| `_mark_connected()` | מעדכן אינדיקטור ● לירוק |
| `_set_room_buttons(enabled)` | enable/disable לכפתורי Create/Join/Refresh |

**stats methods:**

| מתודה | תפקיד |
|-------|-------|
| `_schedule_stats_refresh(delay)` | מתזמן `_fetch_stats` עם `self.after()` |
| `_cancel_stats_refresh()` | מבטל `after()` ממתין |
| `_fetch_stats(manual)` | שולחת GetStats ב-worker thread, קוראת response |
| `_apply_stats(stats)` | מעדכן StringVars בערכים החדשים |

**create/join:**

| מתודה | תפקיד |
|-------|-------|
| `_do_create()` | worker: שולח CreateRoom, קורא RoomCreated, קורא ל-`on_enter_room` |
| `_do_join()` | worker: שולח JoinRoom, קורא RoomJoined, קורא ל-`on_enter_room` |
| `destroy()` | מבטל stats refresh לפני השמדה |

**משתנים חשובים:**

| משתנה | תפקיד |
|-------|-------|
| `_conn` | החיבור הפעיל |
| `_stats_after_id` | מזהה callback ממתין ל-refresh |
| `_stat_vars` | dict של StringVar לכל מונה |
| `_host_var`, `_port_var` | ערכי שדות חיבור |
| `_tor_var` | BooleanVar לתיבת סימון Tor |

---

### 5.8 `client/chat_screen.py` — מסך הצ'אט

**תפקיד:** תצוגת הודעות, רשימת משתמשים, שליחת הודעות וקבצים.

**Class `ChatScreen(ctk.CTkFrame)`:**

**build methods:**

| מתודה | תפקיד |
|-------|-------|
| `_build_topbar()` | Room code + Copy + Leave |
| `_build_main_area()` | תיבת הודעות + רשימת משתמשים |
| `_build_input_bar()` | Attach File + entry + Send |

**message display:**

| מתודה | תפקיד |
|-------|-------|
| `_append(parts)` | מוסיפה חלקי טקסט מתויגים לתיבת ההודעות (CTkTextbox) |
| `_append_system(text)` | הודעת מערכת (אפור נטוי) |
| `_append_chat(from_user, msg)` | הודעת צ'אט (ירוק/כחול לפי שולח) |
| `_append_file_notice(...)` | הודעת קובץ עם כפתור שמור inline |

**user list:**

| מתודה | תפקיד |
|-------|-------|
| `_add_user_label(username)` | יוצרת CTkLabel ומוסיפה לרשימה |
| `_remove_user_label(username)` | מוחקת Label מהרשימה |

**message handling:**

| מתודה | תפקיד |
|-------|-------|
| `_handle_message(msg_type, data)` | dispatcher: IncomingMessage/File/UserJoined/Left/RoomLeft/Stats/Error |
| `_handle_disconnect()` | disconnect לא רצוני (אם `_leaving=False`) |

**actions:**

| מתודה | תפקיד |
|-------|-------|
| `_send_message()` | שולחת הודעה ב-worker thread |
| `_pick_file()` | פותח filedialog, שולח SendFile |
| `_save_file(filename, filedata)` | base64 decode + כתיבה לדיסק |
| `_do_leave()` | שולח LeaveRoom, עוצר receiver, קורא `on_leave(conn)` |
| `_cleanup_and_leave()` | עזיבה שיזם השרת (RoomLeft) |

**משתנים חשובים:**

| משתנה | תפקיד |
|-------|-------|
| `_conn` | החיבור הפעיל |
| `_username` | שם המשתמש בחדר |
| `_room_code` | UUID של החדר |
| `_leaving` | flag למניעת שגיאת disconnect בזמן עזיבה מכוונת |
| `_user_labels` | dict של username → CTkLabel |
| `_textbox` | CTkTextbox של ההודעות |

**צבעי tags בתיבת ההודעות:**

| tag | hex | שימוש |
|-----|-----|-------|
| `username` | `#4a9eff` bold | שם משתמש אחר |
| `self_tag` | `#5cb85c` bold | ההודעות שלי |
| `system` | `#888888` italic | הצטרפות/עזיבה |
| `file_tag` | `#f0a500` italic | קבצים |
| `error` | `#e05252` | שגיאות |

---

### 5.9 `start.sh` / `start.bat` — סקריפטי הפעלה

**תפקיד:** מפעילים את כל הרכיבים בסדר הנכון בחלוניות טרמינל נפרדות.

**פורמט פורטים עם `--nodes N`:**
- Directory: 8000
- Chat Server: 8001
- Entry nodes: 9001 … 9000+N
- Middle nodes: 9101 … 9100+N
- Exit nodes: 9201 … 9200+N

**ארגומנטים:**
- `--nodes N` — מספר צמתים מכל סוג (ברירת מחדל: 1)
- `--debug` — מצב debug בצמתים

---

## 6. בסיס הנתונים

### 6.1 כללי

המערכת משתמשת ב-SQLite לשמירת סטטיסטיקות בלבד. חדרים ומשתמשים **לא** נשמרים ב-DB — הם קיימים בזיכרון בלבד.

**מיקום הקובץ:** `Servers/chat_stats.db` (נוצר אוטומטית בהפעלה הראשונה)

### 6.2 סכמה

```sql
CREATE TABLE IF NOT EXISTS stats (
    key   TEXT PRIMARY KEY NOT NULL,
    value INTEGER NOT NULL DEFAULT 0
);
```

### 6.3 תיאור הטבלה

| עמודה | סוג | מפתח ראשי | חובה | תיאור |
|-------|-----|-----------|------|-------|
| `key` | TEXT | כן | כן | שם המונה |
| `value` | INTEGER | לא | כן (ברירת מחדל 0) | ערך המונה |

### 6.4 מפתחות קבועים

| key | משמעות |
|-----|---------|
| `total_messages` | סה"כ הודעות טקסט שנשלחו |
| `total_files` | סה"כ קבצים שנשלחו |
| `total_users` | סה"כ משתמשים שהתחברו (מצטבר) |
| `total_rooms` | סה"כ חדרים שנוצרו (מצטבר) |

### 6.5 דוגמה לנתונים

```sql
key              | value
-----------------+-------
total_messages   | 147
total_files      | 12
total_users      | 38
total_rooms      | 9
```

### 6.6 גישה לנתונים

- **קריאה:** `db.get_stats()` — SELECT מלא, מחזיר dict.
- **כתיבה:** `db.increment_stat('total_messages')` — `UPDATE ... value = value + 1`.
- כל פעולה מוגנת ב-threading.Lock פנימי.
- `check_same_thread=False` מאפשר שימוש משותף בחיבור ה-SQLite מ-threads מרובים.

---

## 7. סיכום אישי / רפלקציה

פרויקט זה היה אחד האתגרים הטכניים המשמעותיים ביותר שעמדו בפני. בתחילת הדרך הבנתי את העקרון הכללי של ניתוב בצל, אך לא הבנתי לעומק כיצד כל הפרטים הקטנים מתחברים יחד לשלם אחד עובד.

**מה קיבלתי:**
הפרויקט נתן לי הבנה עמוקה של נושאים שתמיד נראו לי מורכבים ומרוחקים — הצפנה בשכבות, ניהול מפתחות, פרוטוקולי תקשורת ברמת TCP, ועיצוב מערכות מרובות-threads. הבנתי שכל "אבסטרקציה" קיימת מסיבה טובה — למשל, ה-`TorSocket` שמתנהג בדיוק כמו socket רגיל מאפשר לכל שאר הקוד לפעול ללא שינויים.

**כלים שאני לוקח להמשך:**
- חשיבה בסגנון "מה כל רכיב יודע ומה הוא לא יודע" — עיקרון הסיווג שמאפשר אנונימיות.
- `threading.Lock` ו-`threading.Event` לניהול thread-safety — כבר ישמשו אותי בכל פרויקט שכולל עיבוד מקבילי.
- מנגנון framing עם 4-byte prefix — עיקרון שחוזר בהרבה פרוטוקולים.
- שיטת ה-select() לאי-חסימת threads ב-socket I/O.

**קשיים ואתגרים:**
הקושי הגדול ביותר היה מנגנון ה-poll. בתחילה לא הבנתי כיצד ניתן לקבל הודעות "דחויות" דרך מעגל שמטבעו הוא request/response. התשובה — לשלוח "שאלה ריקה" ולתת לצומת היציאה לבדוק אם השרת דחף משהו — הייתה אלגנטית ולא ברורה מאליה.

אתגר נוסף היה ניהול ה-race condition בין שני threads שמשתמשים באותו socket בו-זמנית. ה-`_lock` ב-`TorSocket` היה ברור מבחינה לוגית אבל דרש חשיבה זהירה לגבי מתי בדיוק לנעול ומתי לשחרר.

**מה הייתי עושה אחרת:**
לו הייתי מתחיל היום, הייתי כותב בדיקות אוטומטיות לכל פונקצייה לפני שכותב את הקוד עצמו (Test-Driven Development). הבדיקה הידנית של המערכת השלמה דרשה הפעלה של שבעה processes בו-זמנית — עייפה ונטייה לבאגים שקשה לאתר. בדיקות unit לכל פונקציה היו חוסכות הרבה זמן debug.

**מסקנה:**
ניתוב בצל הוא דוגמה מצוינת לעיקרון "need-to-know" — כל צומת יודע רק מה שצריך לו לתפקידו, ולא יותר. זה לא רק עיקרון אבטחה — זה גם עיקרון תכנות טוב: מודולים שיודעים פחות קל יותר לתחזק ולבדוק.

---

## 8. ביבליוגרפיה

Dingledine, R., Mathewson, N., & Syverson, P. (2004). *Tor: The Second-Generation Onion Router*. Proceedings of the 13th USENIX Security Symposium. https://svn.torproject.org/svn/projects/design-paper/tor-design.pdf

Goldschlag, D., Reed, M., & Syverson, P. (1999). *Onion Routing for Anonymous and Private Internet Connections*. Communications of the ACM, 42(2), 39–41.

Python Software Foundation. (2024). *socket — Low-level networking interface*. Python 3.12 Documentation. https://docs.python.org/3/library/socket.html

Python Software Foundation. (2024). *threading — Thread-based parallelism*. Python 3.12 Documentation. https://docs.python.org/3/library/threading.html

Hearn, T., & Tomaka, T. (2023). *pycryptodome Documentation*. https://pycryptodome.readthedocs.io/

CustomTkinter. (2023). *CustomTkinter — A modern and customizable python UI-library*. https://github.com/TomSchimansky/CustomTkinter

---

## 9. נספחים

### נספח א' — תרשים זרימה: בניית מעגל מלאה

```
לקוח                Entry              Middle              Exit              Chat
  │                    │                   │                  │               │
  │── GET_NODES ──────►│ (directory)       │                  │               │
  │◄─ nodes list ──────│                   │                  │               │
  │                    │                   │                  │               │
  │── CIRCUIT_SETUP ──►│                   │                  │               │
  │   (entry payload)  │                   │                  │               │
  │                    │── CIRCUIT_SETUP ─►│                  │               │
  │                    │   (middle payload)│                  │               │
  │                    │                   │── CIRCUIT_SETUP ►│               │
  │                    │                   │   (exit payload) │               │
  │                    │                   │                  │── TCP connect►│
  │                    │                   │                  │◄─ connected ──│
  │                    │                   │◄─── ok ──────────│               │
  │                    │◄──── ok ──────────│                   │               │
  │◄──── ok ───────────│                   │                   │               │
  │                    │                   │                   │               │
  │  מעגל פעיל ✓       │                   │                   │               │
```

### נספח ב' — תרשים זרימה: שליחת הודעה (RELAY) ב-Tor mode

```
לקוח (main thread)    Entry           Middle          Exit      Chat
  │                    │                │               │          │
  │  sendall(payload)  │                │               │          │
  │  [K1(K2(K3(p)))]  │                │               │          │
  │── RELAY(ID_A) ────►│                │               │          │
  │                    │  decrypt K1    │               │          │
  │                    │── RELAY(ID_B) ►│               │          │
  │                    │                │  decrypt K2   │          │
  │                    │                │── RELAY(ID_C)►│          │
  │                    │                │               │  decrypt │
  │                    │                │               │  K3 → p  │
  │                    │                │               │─ send ──►│
  │                    │                │               │◄─ Ack ───│
  │                    │                │               │ enc K3   │
  │                    │                │◄ RELAY_R(ID_C)│          │
  │                    │                │  enc K2       │          │
  │                    │◄ RELAY_R(ID_B)─│               │          │
  │                    │  enc K1        │               │          │
  │◄─ RELAY_R(ID_A) ───│               │               │          │
  │  decrypt K1→K2→K3  │               │               │          │
  │  Ack buffered ✓    │               │               │          │
```

**ID_A, ID_B, ID_C** — שלושה UUID שונים. ID_A נוצר ע"י הלקוח; ID_B נוצר ע"י Entry בשלב ה-CIRCUIT_SETUP; ID_C נוצר ע"י Middle באותו שלב.

### נספח ג' — תרשים זרימה: מנגנון Poll

```
לקוח (receiver thread)   Entry    Middle    Exit       Chat
  │                        │         │         │           │
  │  poll() → RELAY(b'')   │         │         │           │
  │── RELAY ──────────────►│         │         │           │
  │                        │─ RELAY ►│─ RELAY ►│           │
  │                        │         │         │ select(   │
  │                        │         │         │  0.5s)    │
  │                        │         │         │           │
  │  (תרחיש א' — אין הודעה)│         │         │ timeout   │
  │                        │         │         │ b'' ──────│
  │◄── RELAY_RESPONSE(b'')─┤         │         │           │
  │  poll() returns None   │         │         │           │
  │                        │         │         │           │
  │  (תרחיש ב' — יש הודעה)│         │         │           │
  │                        │         │         │◄─IncomingM│
  │                        │         │         │  recv_msg │
  │◄── RELAY_RESPONSE ─────┤         │         │           │
  │   (encrypted message)  │         │         │           │
  │  poll() returns {type} │         │         │           │
  │  on_message(type, data)│         │         │           │
```

### נספח ד' — תרשים thread model

```
                ┌─────────────────────────────────────────┐
                │          תהליך הלקוח (Python)           │
                │                                         │
                │  Main Thread (Tkinter event loop)       │
                │  ├── GUI updates                        │
                │  ├── on_message callbacks               │
                │  └── on_disconnect callbacks            │
                │                                         │
                │  Worker threads (daemon, per operation) │
                │  ├── connect worker                     │
                │  ├── create/join room worker            │
                │  └── stats fetch worker                 │
                │                                         │
                │  Receiver Thread (daemon, persistent)   │
                │  └── _direct_receiver / _tor_receiver   │
                │      (select() / poll() loop)           │
                └─────────────────────────────────────────┘

                ┌─────────────────────────────────────────┐
                │        תהליך כל צומת/שרת               │
                │                                         │
                │  Main Thread — accept loop              │
                │                                         │
                │  Connection threads (daemon, per conn)  │
                │  ├── handle_connection (nodes)          │
                │  └── _handle_connection (chat server)   │
                │                                         │
                │  Health check thread (nodes only)       │
                │  └── health_check_loop (directory)      │
                └─────────────────────────────────────────┘
```

### נספח ה' — למה אין Forward Secrecy (השוואה לTor האמיתי)

| מאפיין | הפרויקט (RSA) | Tor האמיתי (DH/ntor) |
|--------|--------------|----------------------|
| מי קובע את המפתח | הלקוח בלבד | שני הצדדים ביחד |
| Forward Secrecy | לא — דליפת RSA private חושפת כל הסשנים הקודמים | כן — מפתחות ephemeral נמחקים לאחר שימוש |
| round-trip בסetup | לא (צומת רק מפענח) | כן (הצומת שולח חצי DH בחזרה) |
| עלות setup | RSA-OAEP decrypt פעם אחת לצומת | ECDH multiply פעם אחת (מהיר יותר) |
| מזהי מעגל לכל קישור (Per-hop IDs) | **כן — מיושם** (כל צומת מייצר UUID חדש לקישור היוצא) | כן |
